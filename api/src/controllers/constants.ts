import { addGroups, editGroups, loginGroups } from "../models/constants";

export const addGroupAlways = { groups: addGroups, always: true };
export const editGroupAlways = { groups: editGroups, always: true };
export const loginGroupAlways = { groups: loginGroups, always: true };
